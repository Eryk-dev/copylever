# Ralph Progress Log
Started: Mon Mar 30 09:56:48 -03 2026
---

## Codebase Patterns
- Migrations use `IF NOT EXISTS` for idempotency
- Log tables (copy_logs, compat_logs, shopee_copy_logs) use FK `ON DELETE SET NULL` for user_id and org_id
- Log tables use `ON DELETE CASCADE` for core entity tables (users, copy_sellers)
- Next migration number is 017
- Supabase project ID: `wrbrbhuhsaaupqsimkqz`
- TypeScript typecheck: `cd frontend && npx tsc --noEmit`
- Python syntax check: `python3 -c "import py_compile; py_compile.compile('file.py', doraise=True)"`
- ML API functions in `ml_api.py` should be public wrappers around `_get_token` + `_ml_request`/direct httpx calls
- For multipart uploads, use `client.post(..., files={...})` directly (not `_ml_request` which only supports JSON)
- `EditablePhoto` discriminated union in PhotosPage: `existing` (ML photo), `upload` (File + blob URL), `url` (source URL) — used by US-009/010/011
- Blob URLs from `URL.createObjectURL` must be revoked on removal and re-created on restore to avoid memory leaks

## 2026-03-30 - US-001
- Created `app/db/migrations/016_photo_logs.sql` with photo_logs table
- Applied migration to Supabase via MCP
- Updated CHANGELOG.md
- Files changed: `app/db/migrations/016_photo_logs.sql`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - compat_logs was the reference pattern for log tables (bigserial PK, user_id/org_id FKs with SET NULL, JSONB targets, status/counts)
  - shopee_copy_logs (migration 011) shows the newer pattern with `NOT NULL DEFAULT now()` on created_at
  - Indexes: always add org_id index + composite org_id+created_at for log queries
---

## 2026-03-30 - US-002
- Created `app/routers/photos.py` with `GET /api/photos/preview/{item_id}` endpoint
- Registered photos router in `app/main.py`
- Reused `_resolve_item_seller` pattern from compat.py for auto-detecting seller
- Reused SKU extraction logic (seller_custom_field + SELLER_SKU attrs + variation-level)
- Files changed: `app/routers/photos.py` (new), `app/main.py`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - photos.py router follows same structure as compat.py — preview, search-sku, apply, logs
  - _resolve_item_seller is duplicated from compat.py (could be extracted to shared utility later)
  - ML item pictures array has id, url, secure_url, size fields
  - Router registration in main.py: keep imports alphabetical, add `app.include_router()` near the others
---

## 2026-03-30 - US-003
- Added `POST /api/photos/upload` endpoint in `photos.py` — accepts multipart file, validates JPG/PNG and <=10MB
- Added `upload_picture()` public function in `ml_api.py` — forwards file to ML API with 429 retry
- Files changed: `app/routers/photos.py`, `app/services/ml_api.py`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - ML picture upload endpoint: `POST /pictures/items/upload` with multipart `file` field and Bearer auth
  - `_get_token` is private in ml_api.py — create public wrapper functions (like `upload_picture`) instead of importing private methods
  - httpx `client.post(..., files={"file": (filename, bytes, content_type)})` for multipart uploads
  - `_ml_request` helper doesn't support multipart — upload_picture uses `client.post` directly with manual 429 retry loop
  - `_REQUEST_RATE_RETRIES` and `_REQUEST_RATE_BASE_WAIT` are the standard retry constants in ml_api.py
---

## 2026-03-30 - US-004
- Added `POST /api/photos/search-sku` endpoint in `photos.py` — reuses `search_sku_all_sellers` from compat_copier
- Added `thumbnail` field to `search_sku_all_sellers` return data (non-breaking addition for compat endpoint)
- Filters by `can_copy_to` permission for operators, admins get all sellers
- Validates max 50 SKUs per request using Pydantic field_validator
- Files changed: `app/routers/photos.py`, `app/services/compat_copier.py`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - `search_sku_all_sellers` is shared between compat and photos — changes to it affect both endpoints
  - The function already fetches item data via `get_item`, so adding extra fields (thumbnail) is free
  - Permission filtering pattern: `user["role"] != "admin"` → filter by `can_copy_to` from `user.get("permissions", [])`
  - Pydantic `SearchSkuRequest` model with `field_validator` for max SKU count — same pattern used in compat.py
---

## 2026-03-30 - US-005
- Created `app/services/photo_applier.py` with `apply_photos_to_targets()` function
- Creates photo_logs record with status 'processing' before starting
- For each target: calls `update_item()` (PUT /items/{id}) with `{pictures: [...]}` payload
- Updates photo_logs with per-target results, success/error counts, and final status
- Retry on 429 handled by `update_item()` which uses `_ml_request` internally
- Logs errors to `api_debug_logs` via `_log_api_debug` from item_copier
- 1-second pacing between targets to respect ML rate limits
- Files changed: `app/services/photo_applier.py` (new), `CHANGELOG.md`
- **Learnings for future iterations:**
  - `update_item()` in ml_api.py already has 429 retry via `_ml_request` — no need to add retry logic in the service layer
  - ML API updates item photos via `PUT /items/{id}` with `{pictures: [{id: ...}]}` for existing or `{pictures: [{source: url}]}` for URL-based
  - Photo order in the pictures array = display order (first = main photo)
  - `_log_api_debug` from item_copier is reused across services (compat_copier, photo_applier) — it's the standard debug logging pattern
  - Final status values for photo_logs: 'completed' (all ok), 'error' (all failed), 'partial' (mixed)
---

## 2026-03-30 - US-006
- Added `POST /api/photos/apply` endpoint — validates can_copy_to permissions, creates photo_logs record, runs `apply_photos_to_targets` as BackgroundTask, returns log_id immediately
- Added `GET /api/photos/logs` endpoint — returns photo_logs filtered by org_id with username join, status filter, and pagination (default limit=20)
- Refactored `apply_photos_to_targets()` to accept optional `log_id` parameter — when provided (from router), skips creating a duplicate log record
- Pydantic models: `PictureEntry` (id/source), `ApplyTarget` (seller_slug/item_id), `ApplyRequest`
- Files changed: `app/routers/photos.py`, `app/services/photo_applier.py`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - Log creation in router vs service: when endpoint needs to return log_id immediately, create the log in the router and pass log_id to the background task (same pattern as compat.py)
  - `photo_applier.apply_photos_to_targets` now supports both modes: create own log (log_id=None) or use existing log (log_id=N)
  - Permission validation for operators: check `can_copy_to` per target seller_slug, admins bypass
  - Logs endpoint pattern: join `users(username)`, flatten the nested users_data into `row["username"]`, filter by role (super_admin=all, admin=org, operator=own)
  - `model_dump(exclude_none=True)` on Pydantic models to convert PictureEntry to dict with only set fields
---

## 2026-03-30 - US-007
- Created `frontend/src/pages/PhotosPage.tsx` — full page with MLB input, auto-preview (debounced 600ms), photo grid (~150px CSS grid), SKU badges, seller detection
- Updated `frontend/src/App.tsx` — added 'photos' to View type, imported PhotosPage, added 'Fotos' tab visible to all authenticated users, renders PhotosPage with sellers + headers props
- Updated `CHANGELOG.md`
- Files changed: `frontend/src/pages/PhotosPage.tsx` (new), `frontend/src/App.tsx`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - `useRef<T>(null)` is required in React 19 strict mode — `useRef<T>()` (no arg) causes TS2554 error
  - PhotosPage follows CompatPage pattern: Props = `{sellers, headers}`, uses `Card` from CopyPage, `useToast`, `parseItemId`
  - Photos preview endpoint returns `{id, title, thumbnail, pictures: [{id, url, secure_url, size}], skus: [], seller}`
  - CSS grid with `gridTemplateColumns: 'repeat(auto-fill, minmax(150px, 1fr))'` + `aspectRatio: '1'` for photo thumbnails
  - Tab visibility: 'photos' is pushed unconditionally (all authenticated users), unlike 'copy' (needs permissions) or 'compat' (needs can_run_compat)
  - `handlePreviewRef` pattern: store latest callback in ref to avoid stale closure in debounced useEffect
---

## 2026-03-30 - US-008
- Added photo delete (X button overlay), restore (Removidas section), and drag & drop reorder to PhotosPage
- `activePhotos` and `removedPhotos` state arrays initialized from `preview.pictures` on preview load
- HTML5 Drag and Drop: `onDragStart` stores index in `dataTransfer`, `onDrop` splices and reinserts in array
- Remove button hidden when only 1 photo left (cannot remove last photo)
- Removed photos shown in separate grid with dashed border, opacity 0.5, and "Restaurar" button
- Visual feedback: dragged item at 0.4 opacity, drop target gets accent border
- Files changed: `frontend/src/pages/PhotosPage.tsx`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - HTML5 DnD on images: set `draggable={false}` on `<img>` and `pointerEvents: 'none'` to prevent browser default image drag
  - `setActivePhotos` inside `handleRemovePhoto` uses functional update to avoid stale closures — calling `setRemovedPhotos` inside the updater is fine since React batches state
  - Drag & drop: store source index via `dataTransfer.setData('text/plain', ...)`, read in `onDrop` via `getData`
  - Photos grid uses `activePhotos` state (not `preview.pictures` directly) — future stories (US-009, US-011) will add to this array
---

## 2026-03-30 - US-009
- Added `EditablePhoto` discriminated union type: `existing` | `upload` | `url` — replaces raw `PhotoPicture` in activePhotos/removedPhotos state
- Added "Adicionar foto" button → file picker (multiple, accept JPG/PNG, validates 10MB max)
- Added URL input field with "Adicionar" button (Enter key support)
- Upload files get local preview via `URL.createObjectURL`, properly revoked on remove, re-created on restore
- New photos have dashed accent border and "Nova" badge overlay
- Helper functions: `getPhotoKey()`, `getPhotoSrc()`, `isNewPhoto()`, `nextTempId()`
- Files changed: `frontend/src/pages/PhotosPage.tsx`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - `EditablePhoto` type is the central type for US-011 (apply flow) — upload type has `file: File` for FormData upload, url type has `source` for ML `{source: url}` payload
  - `URL.createObjectURL` creates blob URLs that must be revoked to prevent memory leaks — revoke on remove, re-create on restore
  - `nextTempId()` uses module-level counter + Date.now() for uniqueness — needed because React keys must be stable
  - File input `e.target.value = ''` after read is needed to allow re-selecting the same file
  - The `handleRestorePhoto` must not mutate state — spread operator `{...item, previewUrl: ...}` creates a new object
---

## 2026-03-30 - US-010
- Added SKU search and target selection to PhotosPage
- SKU input auto-populates with first detected SKU from preview
- POST /api/photos/search-sku with {skus: [sku]} — source item excluded from results
- Results displayed as list with checkbox, thumbnail, item_id, title, seller name
- All results selected by default, "Selecionar todos" toggle, counter "X de Y anuncios selecionados"
- Empty state message when search returns no results, error display on API failure
- Added `SkuSearchResult` interface with thumbnail field
- Files changed: `frontend/src/pages/PhotosPage.tsx`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - `selectedTargets` uses `Set<string>` with composite key `seller_slug:item_id` for O(1) lookups — needed because same item_id could appear in multiple sellers
  - `hasSearched` boolean flag distinguishes "no search performed yet" from "search returned 0 results" for empty state rendering
  - Source item exclusion is done client-side by filtering `r.item_id !== preview?.id` — simpler than server-side and handles edge cases where item appears in multiple sellers
  - Search results include `thumbnail` field (added in US-004 to `search_sku_all_sellers`) — displayed as 40x40 image in result rows
  - For US-011: `selectedTargets` Set + `skuSearchResults` array provide the data needed to build the `targets` array for POST /api/photos/apply
---

## 2026-03-30 - US-011
- Added apply photos flow to PhotosPage: uploads new photos (type 'upload') via POST /api/photos/upload, builds pictures payload, calls POST /api/photos/apply
- 'Aplicar fotos' button enabled only when targets are selected AND at least 1 photo exists
- Button shows 'Aplicando...' with spinner during upload+apply, disabled during operation
- After apply: polls GET /api/photos/logs every 3s for the created log_id, shows summary toast 'X sucesso, Y erros' when complete
- Poll interval cleaned up on component unmount via ref
- Files changed: `frontend/src/pages/PhotosPage.tsx`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - `headers()` from useAuth includes `Content-Type: application/json` — for multipart uploads, extract only `X-Auth-Token` header to let browser set correct Content-Type with boundary
  - Apply endpoint returns `{log_id, status, message}` immediately (BackgroundTask) — need polling to get final results
  - `pollRef` pattern: store `setInterval` return value in a ref, clear on unmount via useEffect cleanup, also clear before starting new poll
  - `canApply` derived value (not state): `preview && selectedTargets.size > 0 && activePhotos.length > 0 && !applying`
  - US-012 (logs section) will need its own polling mechanism — US-011 only polls to show the summary toast after apply, not for the logs UI
---

## 2026-03-30 - US-012
- Added photo operation history section to PhotosPage with collapsible Card, table view, expandable rows, status filter, polling, and pagination
- `PhotoLog` interface with `PhotoLogTarget` for per-target details
- `loadLogs` with AbortController pattern (same as CompatPage), `loadMoreLogs` for pagination (20 per page)
- Status filter tabs: Todos, Em andamento, Sucesso, Erro
- Polling every 5s while any log has status 'processing' or 'pending' (uses `loadLogsRef` pattern to avoid stale closures)
- Expandable rows: click a row to see per-target results (item_id, seller name, ok/error indicator)
- `PhotoStatusBadge` component: colored badges for completed/error/partial/processing/pending with pulse animation for active statuses
- `LogRow` component: handles row + expanded detail rendering
- Integrated with apply flow: logs refresh after apply call + after apply completes
- Files changed: `frontend/src/pages/PhotosPage.tsx`, `CHANGELOG.md`
- **Learnings for future iterations:**
  - photo_logs status values differ from compat_logs: 'completed'/'error'/'partial'/'processing'/'pending' (compat uses 'success'/'in_progress')
  - `LogRow` as a separate component avoids cluttering the main render — it handles both the data row and the expandable detail row via React Fragment
  - `loadLogsRef` pattern needed for polling interval — `loadLogs` changes on every render due to `headers()` dependency, but the ref always points to latest
  - Renamed shadow variable `logs` to `allLogs` inside apply poll handler to avoid conflict with component-level `logs` state
  - History section is always visible (not gated behind `preview`) — users can check history without loading a new item
---
