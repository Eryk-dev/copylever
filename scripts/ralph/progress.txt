# Ralph Progress Log
Started: Thu Mar  5 10:43:47 -03 2026
---

## Codebase Patterns
- Use `IF NOT EXISTS` for all CREATE TABLE, CREATE INDEX in SQL migrations
- Use `ADD COLUMN IF NOT EXISTS` for ALTER TABLE in migrations
- Supabase uses service_role key (bypass RLS) via `get_db()` singleton
- Auth dependencies: `require_user()` → `require_admin()` → `require_super_admin()`
- Session pattern: `secrets.token_urlsafe(32)`, 7 day expiry, stored in `user_sessions`
- Password hashing: `bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")`
- Error messages in Portuguese (user-facing), English (logs/code)
- Python typecheck: `python3 -m py_compile <file>` or `python3 -c "import ast; ..."`
- Frontend typecheck: `cd frontend && npx tsc --noEmit`
---

## 2026-03-05 - US-001
- Created migration 007_multi_tenant.sql: orgs table, org_id columns on all tables, indexes
- Files changed: app/db/migrations/007_multi_tenant.sql
- **Learnings for future iterations:**
  - Existing migrations use numbered prefix pattern (001 through 006)
  - Use IF NOT EXISTS everywhere for idempotent migrations
---

## 2026-03-05 - US-002
- Created migration 008_backfill_org_data.sql: Lever Money org, backfill, constraints
- Files changed: app/db/migrations/008_backfill_org_data.sql
- **Learnings for future iterations:**
  - Deterministic UUID for Lever Money: '00000000-0000-0000-0000-000000000001'
  - Use ON CONFLICT DO NOTHING for idempotent inserts
  - Drop old unique constraints before creating org-scoped ones
---

## 2026-03-05 - US-003
- Added Stripe config fields to Settings class
- Files changed: app/config.py
---

## 2026-03-05 - US-004
- Updated require_user() to return org_id, is_super_admin, email
- Added require_super_admin() and require_active_org() dependencies
- Files changed: app/routers/auth.py
- **Learnings for future iterations:**
  - require_user returns: {id, username, email, role, org_id, is_super_admin, can_run_compat, permissions}
  - require_active_org skips check for super_admin users
---

## 2026-03-05 - US-005
- Added POST /api/auth/signup endpoint with SignupRequest model
- Creates org + user + session in one flow
- Validates email uniqueness, password length, company_name presence
- Returns {token, user, org} matching the acceptance criteria
- Files changed: app/routers/auth.py
- **Learnings for future iterations:**
  - Signup creates org first, then user with org_id reference
  - Username is set to email for new signups
  - New orgs get payment_active=true by default (billing enforcement deferred)
  - auth_logs insert includes org_id for signup events
---
